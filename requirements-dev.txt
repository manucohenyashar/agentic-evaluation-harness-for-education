# Development / test dependencies. Test-plan §4.7 declares the tooling; entries are added
# here as the story that needs them lands, so the fast tier stays installable in seconds.
#
#   python -m venv .venv
#   .venv/Scripts/python -m pip install -r requirements-dev.txt     # Windows
#   .venv/bin/python -m pip install -r requirements-dev.txt         # POSIX

pytest>=8.0

# §4.6: the unit suite runs SHUFFLED — "a suite that fails under shuffle has hidden shared
# state, and in a system with a single-writer thread and a work ledger that is a real defect
# worth surfacing."
pytest-randomly>=3.15

# §4.7's property/fuzz tool. Added by TS-03 (issue #7), which is the first story carrying
# Property-level cases — `TC-CONF-15` and `TC-CONF-16`. TS-00 guessed TS-56 would be first;
# §8.2 shows TS-56 is the *cross-module* property suite (FUZZ-06, FUZZ-07), which is later.
# Profiles `ci` and `nightly` are registered in tests/conftest.py, per §4.7's command table.
hypothesis>=6.100
